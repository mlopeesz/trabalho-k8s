apiVersion: v1
kind: Service
metadata:
  name: minha-app-service
  namespace: faculdade
spec:
  selector:
    app: minha-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
pipeline {
    agent any

    stages {
        stage('Run App') {
            steps {
                script {
                    docker.image('node:18').inside {
                        sh 'npm install'
                        sh 'npm test || true'
                    }
                }
            }
        }
    }
}
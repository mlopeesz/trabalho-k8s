pipeline {
    agent any

    stages {
        stage('Run App') {
            steps {
                sh '''
                    apt update
                    apt install -y nodejs npm
                    npm install
                '''
            }
        }
    }
}